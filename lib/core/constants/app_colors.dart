import 'package:flutter/material.dart';

/// Centralized application color palette approved for Yahya Mohamed's portfolio.
abstract final class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF07111F);
  static const Color sectionBackground = Color(0xFF0A1625);

  // Cards
  static const Color card = Color(0xFF102033);
  static const Color cardHover = Color(0xFF152A42);

  // Accents & Brand
  static const Color primaryCyan = Color(0xFF18B6F6);
  static const Color secondaryViolet = Color(0xFF6C63FF);
  static const Color glow = Color(0xFF38CFFF);

  // Text
  static const Color primaryText = Color(0xFFF4F8FC);
  static const Color secondaryText = Color(0xFF91A4B7);

  // Borders & Accents
  static const Color border = Color(0xFF21364B);
  static const Color success = Color(0xFF2DD4A8);
}
